<h2>All events</h2>
{{#each event}}
<div class="container">
  <article id="list">
    <p>{{this.name}}</p>
    <p>{{this.description}}</p>
    <p>{{this.location}}</p>
    <p><img href="{{this.image}}" /></p>
  </article>
</div>
{{/each}}